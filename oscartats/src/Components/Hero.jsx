import React from 'react';
// Import hero data
import { heroData } from '../data'
// Import motion (npm install framer-motion)
import { motion } from "framer-motion";
//import fadein
import {fadeIn} from '../variants'

const Hero = () => {
  return <div className='bg-pink-200'>Hero</div>;
};

export default Hero;